[project]
name = "starproject"
version = "0.1.0"
description = ""
authors = [
    {name = "Flor",email = "floripanozzo@gmail.com"}
]
requires-python = ">=3.13"
dependencies = [
    "fastapi (>=0.136.1,<0.137.0)",
    "uvicorn (>=0.46.0,<0.47.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pre-commit (>=4.6.0,<5.0.0)",
    "ruff (>=0.15.17,<0.16.0)",
    "pytest (>=9.1.0,<10.0.0)",
    "mypy (>=2.1.0,<3.0.0)",
    "bandit (>=1.9.4,<2.0.0)"
]
